apiVersion: v1
kind: Route
metadata:
  name: transaction-service
  namespace: bank-infra
spec:
  port:
    targetPort: 9080
  to:
    kind: Service
    name: transaction-service
    weight: 100
  wildcardPolicy: None
